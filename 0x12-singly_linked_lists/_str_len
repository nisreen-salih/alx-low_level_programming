#include "lists.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: string
 *
 * Return: length
 */

int _strlen(char *s)
{
        if (*s == '\0')
        {
                return (0);
        }
        else
        {
        return (1 + _strlen_recursion(s + 1));
        }
}
