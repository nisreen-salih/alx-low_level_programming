#include <stdio.h>

/**
 * main - Entry point
 *
 * description: alphabet
 *
 * Return: always 0
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putchar (ch);
		ch++;
	}
	return (0);
}
