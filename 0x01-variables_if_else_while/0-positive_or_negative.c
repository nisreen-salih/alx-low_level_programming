#include <stdio.h>

/**
* main – Entry point
*
* discerption : task 0
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int n;

	if (n > 0)
		printf("is positive");
	else if (n == 0)
		printf("is zero");
	else
		printf("is negative");
	return (0);
}
